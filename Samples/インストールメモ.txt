１．nodejpsのインストール
　　C:\Program Files\にインストール

２．環境設定
　npm config set prefix "C:\npm_global"
  [System.Environment]::SetEnvironmentVariable("NODE_PATH", (npm root -g), "User")
  [System.Environment]::SetEnvironmentVariable("PLAYWRIGHT_BROWSERS_PATH",(Join-Path (npm root -g) "playwright\browsers"), "User")

３．PlayWrightのインストール
　npm install -g playwright
　npx playwright install
  npm install -g yargs


