#$NODE_HOME = "D:\nodejs"
$PROJECT_ROOT = "D:\OfficeTools"


#１．nodejpsのインストール
#　　$NODE_HOMEにインストール

#２．環境設定
　npm config set prefix (Join-Path "$PROJECT_ROOT" "npm")

 $nodePath =  npm root -g
 [System.Environment]::SetEnvironmentVariable("NODE_PATH", $nodePath, "Machine")
 [System.Environment]::SetEnvironmentVariable("NODE_PATH", $nodePath, "User")
 
 $browserPath = Join-Path $PROJECT_ROOT "npm\browser"
 [System.Environment]::SetEnvironmentVariable("PLAYWRIGHT_BROWSERS_PATH",$browserPath, "Machine")
 [System.Environment]::SetEnvironmentVariable("PLAYWRIGHT_BROWSERS_PATH",$browserPath, "User")

３．PlayWrightのインストール
　npm install -g playwright
　npx playwright install
  npm install -g yargs



$PROJECT_ROOT = "D:\OfficeTools"
npm config set prefix $PROJECT_ROOT

$nodePath = Join-Path $PROJECT_ROOT "lib\node_modules"
[System.Environment]::SetEnvironmentVariable("NODE_PATH", $nodePath, "Machine")
[System.Environment]::SetEnvironmentVariable("NODE_PATH", $nodePath, "User")

npm install -g playwright

ls $PROJECT_ROOT




------

１．nodejpsのインストール
　　D:\nodejs\にインストール

２．環境設定
  $npm_root = "C:\Program Files\npm"
　npm config set prefix $npm_root
  $node_path =  npm root -g
  $browsers_path = Join-Path $npm_root "browsers"
  [System.Environment]::SetEnvironmentVariable("NODE_PATH", $node_path, "User")
  [System.Environment]::SetEnvironmentVariable("PLAYWRIGHT_BROWSERS_PATH",$browsers_path, "User")
  [System.Environment]::SetEnvironmentVariable("NODE_PATH", "$node_path", "Machine")
  [System.Environment]::SetEnvironmentVariable("PLAYWRIGHT_BROWSERS_PATH",$browsers_path, "Machine")

３．PlayWrightのインストール
  $npm_root = "C:\Program Files\npm"
  cd $npm_root
  npm init -y
　npm install -D playwright
　npx playwright install

  npm install -D yargs


  [System.Environment]::GetEnvironmentVariable("PLAYWRIGHT_BROWSERS_PATH", "User")
