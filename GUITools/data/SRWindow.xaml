<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SRWindow" Width="600" Height="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- メインペイン -->
        <StackPanel Orientation="Vertical" Margin="10" Grid.Row="0">
            <ContentControl x:Name="SearchComboHost" Margin="0,0,0,10"/>
            <ContentControl x:Name="ResultGridHost"/>
        </StackPanel>

        <!-- ステータスバー -->
        <DockPanel Grid.Row="1" VerticalAlignment="Bottom" LastChildFill="True" Margin="2,0,2,2">
            <TextBlock x:Name="StatusText" Text="準備完了"
                       VerticalAlignment="Center" FontSize="12" Padding="2,2"/>
        </DockPanel>
    </Grid>
</Window>